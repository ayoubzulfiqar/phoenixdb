# Gradle settings for the PhoenixDB Android plugin module.
#
# AndroidX is required by the Flutter embedding; Jetifier is not, since this
# module ships no Java/Kotlin code and depends on no legacy support libraries.
android.useAndroidX=true
android.enableJetifier=false
org.gradle.jvmargs=-Xmx2048m
